"""HAL Agent — raccoglitore desktop (Mac/Windows) per HAL-SaaS.

Scrappa feed RSS/Substack/Reddit/YouTube (con trascrizioni) usando l'IP
residenziale del PC dell'utente e invia le novità al SaaS via API.
Opzionalmente fa da ponte verso un LLM locale (Ollama / LM Studio).
"""
__version__ = "0.2.3"
